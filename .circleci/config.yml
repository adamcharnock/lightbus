version: 2
jobs:
  build:
    docker:
      - image: themattrix/tox
      - image: circleci/postgres:9.6.2
        environment:
          POSTGRES_USER: postgres
          POSTGRES_DB: postgres
      - image: redis:5

    steps:
      - checkout
      - run: tox -- --cov=lightbus
