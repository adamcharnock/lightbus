[pytest]
markers =
    unit: Unit tests (fast)
    integration: Integration tests (slow)
    reliability: Reliability tests (slower)
    benchmark: Benchmarking results (slower)
python_files =
    test_*.py
    benchmark_*.py
python_functions =
    test_*
    benchmark_*
asyncio_mode = auto
